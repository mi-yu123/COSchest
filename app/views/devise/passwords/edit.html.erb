<div class="max-w-md mx-auto mt-8 p-6 bg-white rounded-lg shadow-md">
  <h2 class="text-2xl font-semibold mb-6 text-center"><%= t('.change_your_password') %></h2>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <%= f.hidden_field :reset_password_token %>

    <div class="mb-4">
      <%= f.label :password, t('.new_password'), class: "block text-gray-700 font-medium mb-2" %>
      <% if @minimum_password_length %>
        <span class="text-sm text-gray-500 block mb-2">
          ※ パスワードは<%= @minimum_password_length %>文字以上で入力してください
        </span>
      <% end %>
      <%= f.password_field :password, autofocus: true, autocomplete: "new-password",
          class: "w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" %>
    </div>

    <div class="mb-6">
      <%= f.label :password_confirmation, t('.confirm_new_password'), 
          class: "block text-gray-700 font-medium mb-2" %>
      <%= f.password_field :password_confirmation, autocomplete: "new-password",
          class: "w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" %>
    </div>

    <div class="mb-6">
      <%= f.submit t('.change_my_password'),
          class: "w-full py-3 px-4 bg-blue-600 text-white font-medium rounded-md hover:bg-blue-700 transition duration-200" %>
    </div>
  <% end %>

  <div class="text-center space-y-4">
    <%= link_to t('devise.shared.links.sign_in'), new_session_path(resource_name),
        class: "text-blue-600 hover:text-blue-800 underline block" %>
  </div>
</div>

<div class="max-w-md mx-auto mt-4 p-4 bg-gray-50 rounded-lg">
  <p class="text-sm text-gray-600">
    セキュリティのため、新しいパスワードは以前のものと異なるものを設定してください。
  </p>
</div>