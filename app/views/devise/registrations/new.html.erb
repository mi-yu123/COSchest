<h2 class="text-2xl font-semibold mb-6">新規登録</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "space-y-4" }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email, t('devise.registrations.new.email'), class: "block text-gray-700 font-medium" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full p-3 border border-gray-300 rounded-md" %>
  </div>

  <div class="field">
    <%= f.label :password, t('devise.registrations.new.password'), class: "block text-gray-700 font-medium" %>
    <% if @minimum_password_length %>
    <em class="rext-sm text-gray-500">(<%= @minimum_password_length %>文字以上で入力してください)</em>
    <% end %>
    <%= f.password_field :password, autocomplete: "new-password", class: "w-full p-3 border border-gray-300 rounded-md" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation, t('devise.registrations.new.password_confirmation'), class: "block text-gray-700 font-medium" %>
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full p-3 border border-gray-300 rounded-md" %>
  </div>

  <div class="actions">
    <%= f.submit t('devise.registrations.new.submit'), class: "w-full p-3 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>
<% end %>
