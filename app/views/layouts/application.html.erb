<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for(:title) || "COSchest" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
  </head>

  <body class="bg-white-100">
  <header class="bg-white-800 text-gray p-4 shadow-md">
    <div class="container mx-auto flex justify-between items-center">
      <!-- ロゴ -->
      <a href="/" class="flex items-conter">
        <img src="<%= asset_path('coschest_logo.png') %>" alt="COSchestロゴ" class="h-8 w-auto">
      </a>

      <!-- ユーザーアイコン -->
      <div class="relative">
        <button class="bg-white-600 rounded-full p-2">
          <img src="<%= asset_path('default_user_icon.png')  %>" alt="ユーザーアイコン" class="h-8 w-8 rounded-full">
        </button>
      </div>
    </div>
  </header>

  <div class="container mx-auto p-4">
      <%= yield %>
    </div>
  </body>

  <footer class="bg-white-800 text-gray py-6">
  <div class="container mx-auto text-center">
    <ul class="space-y-4 sm:space-y-0 sm:flex sm:space-x-6 sm:justify-center">
      <li><a href="#" class="hover:underline">利用規約</a></li>
      <li><a href="#" class="hover:underline">お問い合わせ</a></li>
      <li><a href="#" class="hover:underline">プライバシーポリシー</a></li>
    </ul>
  </div>
</footer>

</html>
